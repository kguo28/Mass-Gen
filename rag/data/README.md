Place RAG source files here when they should ship with the deployed chatbot.

The Vercel fallback retriever searches text-like files such as Markdown, TXT,
CSV, JSON, HTML, TS, and TSX. Convert PDF or DOCX sources to text/Markdown
before adding them here, or host a separate vector database for richer retrieval.
